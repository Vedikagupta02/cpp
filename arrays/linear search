#include <iostream>
using namespace std;

bool search(int arr[],int size,int key){
    for (int i=0;i<size;i++){
        if (arr[i]==key){
            return 1;
        }
    }
    return 0;
}



int main() {
    // Write C++ code here
    
    int key;
    cin>>key;
    
    int arr[5]={3, 7 , 11, 90, 54};
    
    bool found=search(arr,5,key);
    
    if (found){
        cout<<"found"<<endl;
    }
    
    else{
        cout<<"not found"<<endl;
    }
    
    return 0;
    
}
